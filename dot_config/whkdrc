.shell pwsh

# Reload whkd configuration
# control + alt + o                 : taskkill /f /im whkd.exe && start /b whkd # if shell is cmd
control + alt + o                 : taskkill /f /im whkd.exe; Start-Process whkd -WindowStyle hidden # if shell is pwsh / powershell
control + alt + shift + o         : komorebic reload-configuration

# App shortcuts - these require shell to be pwsh / powershell
# The apps will be focused if open, or launched if not open
control + alt + b                 : if ($wshell.AppActivate('Firefox') -eq $False) { start firefox }
# control + alt + b                 : if ($wshell.AppActivate('Chrome') -eq $False) { start chrome }

control + alt + q                 : komorebic close
control + alt + m                 : komorebic minimize

# Focus windows
control + alt + h                 : komorebic focus left
control + alt + j                 : komorebic focus down
control + alt + k                 : komorebic focus up
control + alt + l                 : komorebic focus right
control + alt + shift + oem_4     : komorebic cycle-focus previous # oem_4 is [
control + alt + shift + oem_6     : komorebic cycle-focus next # oem_6 is ]

# Move windows
control + alt + shift + h         : komorebic move left
control + alt + shift + j         : komorebic move down
control + alt + shift + k         : komorebic move up
control + alt + shift + l         : komorebic move right
control + alt + shift + return    : komorebic promote

# Stack windows
control + alt + left              : komorebic stack left
control + alt + down              : komorebic stack down
control + alt + up                : komorebic stack up
control + alt + right             : komorebic stack right
control + alt + oem_1             : komorebic unstack # oem_1 is ;
control + alt + oem_4             : komorebic cycle-stack previous # oem_4 is [
control + alt + oem_6             : komorebic cycle-stack next # oem_6 is ]

# Resize
control + alt + oem_plus          : komorebic resize-axis horizontal increase
control + alt + oem_minus         : komorebic resize-axis horizontal decrease
control + alt + shift + oem_plus  : komorebic resize-axis vertical increase
control + alt + shift + oem_minus : komorebic resize-axis vertical decrease

# Manipulate windows
control + alt + t                 : komorebic toggle-float
control + alt + shift + f         : komorebic toggle-monocle

# Window manager options
control + alt + shift + r         : komorebic retile
control + alt + p                 : komorebic toggle-pause

# Layouts
control + alt + x                 : komorebic flip-layout horizontal
control + alt + y                 : komorebic flip-layout vertical

# Workspaces
control + alt + 1                 : komorebic focus-workspace 0
control + alt + 2                 : komorebic focus-workspace 1
control + alt + 3                 : komorebic focus-workspace 2
control + alt + 4                 : komorebic focus-workspace 3
control + alt + 5                 : komorebic focus-workspace 4
control + alt + 6                 : komorebic focus-workspace 5
control + alt + 7                 : komorebic focus-workspace 6
control + alt + 8                 : komorebic focus-workspace 7

# Move windows across workspaces
control + alt + shift + 1         : komorebic move-to-workspace 0
control + alt + shift + 2         : komorebic move-to-workspace 1
control + alt + shift + 3         : komorebic move-to-workspace 2
control + alt + shift + 4         : komorebic move-to-workspace 3
control + alt + shift + 5         : komorebic move-to-workspace 4
control + alt + shift + 6         : komorebic move-to-workspace 5
control + alt + shift + 7         : komorebic move-to-workspace 6
control + alt + shift + 8         : komorebic move-to-workspace 7
