# -----------------------------------------------------
# Plugins
# -----------------------------------------------------

# Fish handles plugins differently, so no direct translation is needed.
# You can install plugins using Fisher or other plugin managers.
